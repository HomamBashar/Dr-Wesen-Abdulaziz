{$DUCKDNS_DOMAIN} {
	reverse_proxy clinic:8001
	encode gzip
}

# Caddy يتصل تلقائياً بـ Let's Encrypt، يثبت أن هذا السيرفر يملك هذا
# النطاق فعلاً (عبر تحدي HTTP على المنفذ 80)، ثم يصدر شهادة HTTPS
# مجانية ويجدّدها تلقائياً كل 90 يوماً — بدون أي تدخل يدوي لاحقاً.
